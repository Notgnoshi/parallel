#pragma once

#include <stdbool.h>
#include <stdint.h>

/**
 * @brief
 *
 * @param pid
 * @param z
 */
bool circuit_one( uint32_t pid, uint32_t z );

/**
 * @brief
 *
 * @param pid
 * @param z
 */
bool circuit_two( uint32_t pid, uint32_t z );
